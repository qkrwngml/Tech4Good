import styled from "styled-components";
import { motion } from "framer-motion";

const Container = styled.div``;

const NoticeBottom = ({ onClick }) => {
  return <Container onClick={onClick}></Container>;
};

export default NoticeBottom;
